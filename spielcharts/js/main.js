/*$(document).ready(function() {
  $(':text').blur(function(e) {
    var intRegex = /[0-9]/;
    if($(this).match(intRegex)) {
      $(this).addClass('invalide');
      e.preventDefault();
    }

  });

})
*/
